# Declarative Automation Bundle configuration, more info here: https://docs.databricks.com/en/dev-tools/bundles/settings.html
bundle:
  name: "btt_dabs_test"

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  # The 'dev' target, for development purposes. This target is the default.
  dev:
    mode: development
    default: true
    workspace:
      host: https://adb-3438839487639471.11.azuredatabricks.net

  # The 'prod' target, used for production deployment.
  prod:
    mode: production
    workspace:
      host: https://adb-3438839487639471.11.azuredatabricks.net
      # We always use /Users/mehak.juneja@databricks.com for all resources to make sure we only have a single copy.
      root_path: /Users/mehak.juneja@databricks.com/.bundle/${bundle.name}/${bundle.target}
    run_as:
      # This runs as mehak.juneja@databricks.com in production. We could also use a service principal here,
      # see https://docs.databricks.com/dev-tools/bundles/permissions.html.
      user_name: mehak.juneja@databricks.com
